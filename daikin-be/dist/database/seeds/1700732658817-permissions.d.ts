import { DataSource } from "typeorm";
import { Seeder } from "typeorm-extension";
export default class Permissions1700732658817 implements Seeder {
    run(dataSource: DataSource): Promise<any>;
}
