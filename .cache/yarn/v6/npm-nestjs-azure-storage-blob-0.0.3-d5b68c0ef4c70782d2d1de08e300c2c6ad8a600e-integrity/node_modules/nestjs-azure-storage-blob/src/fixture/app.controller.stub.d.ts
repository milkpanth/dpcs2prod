import { AzureStorageBlobService } from '../lib';
export declare class AppController {
    private readonly storageBlobService;
    constructor(storageBlobService: AzureStorageBlobService);
    getSas(containerName?: string, fileName?: string): Promise<{
        accountSas: import("../lib").GetAccountSasUrlResponse;
        containerSas: import("../lib").GetContainerSasUrlResponse;
        blobSas: import("../lib").GetBlockBlobSasUrlResponse;
    }>;
    upload(containerName?: string, fileName?: string): Promise<{
        blobSas: import("../lib").GetBlockBlobSasUrlResponse;
        status: number;
    }>;
}
